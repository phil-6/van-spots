<div class="container">
  <div class="col-sm-10 col-sm-offset-1 col-xs-12 blog-content">
    <% @spots.each do |spot| %>
      <div class="col-xs-12">
        <div class="text-center">
          <h2><%= spot.name %></h2>
          <h6><%= spot.created_at.strftime('%b %d, %Y') %></h6>

        </div>

        <div>
          <%= spot.description %>
        </div>

<!--        TODO only display when logged in-->
<!--        <div class="text-center">-->
          <%#= link_to "READ MORE", spot_path(spot) %>
<!--        </div>-->
        <br>
      </div>
    <% end %>
  </div>
</div>